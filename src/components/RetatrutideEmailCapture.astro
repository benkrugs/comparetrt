---
// Netlify Forms component for retatrutide email capture
// Submissions go to Netlify dashboard (Forms tab)
---

<div class="email-capture-netlify">
  <h3>📧 Get Notified When Retatrutide Is Available</h3>
  <p>Join 10,000+ people waiting for FDA approval. We'll email you as soon as retatrutide is available through telemedicine providers.</p>
  
  <form 
    name="retatrutide-waitlist" 
    method="POST" 
    data-netlify="true"
    netlify-honeypot="bot-field"
  >
    <!-- Hidden fields for Netlify -->
    <input type="hidden" name="form-name" value="retatrutide-waitlist" />
    <p hidden>
      <label>Don't fill this out: <input name="bot-field" /></label>
    </p>

    <div class="form-fields">
      <input 
        type="email" 
        name="email" 
        class="email-input" 
        placeholder="Enter your email" 
        required
      />
      <button type="submit" class="email-submit">Notify Me</button>
    </div>
    
    <p class="privacy-note">We respect your privacy. Unsubscribe anytime.</p>
  </form>
</div>

<style>
  .email-capture-netlify {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
    color: white;
    border-radius: 12px;
    padding: 40px 32px;
    text-align: center;
    margin: 48px 0;
  }
  
  .email-capture-netlify h3 {
    font-size: 1.8rem;
    font-weight: 700;
    margin-bottom: 12px;
  }
  
  .email-capture-netlify p {
    font-size: 1.05rem;
    opacity: 0.95;
    margin-bottom: 24px;
  }
  
  .form-fields {
    display: flex;
    gap: 12px;
    max-width: 500px;
    margin: 0 auto 16px;
    flex-wrap: wrap;
    justify-content: center;
  }
  
  .email-input {
    flex: 1;
    min-width: 250px;
    padding: 14px 20px;
    border-radius: 8px;
    border: none;
    font-size: 1rem;
  }
  
  .email-submit {
    padding: 14px 32px;
    background: #1e293b;
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
  }
  
  .email-submit:hover {
    background: #0f172a;
    transform: translateY(-1px);
  }
  
  .privacy-note {
    font-size: 0.85rem;
    opacity: 0.8;
    margin: 0;
  }
</style>
